spring:
  cloud:
    skipper:
      server:
        packageRepositories:
         -
           name: test
           url: "classpath:"
        platform:
          local:
            accounts:
              localDev:
                shutdownTimeout: 0
                javaOpts: "-Dtest=foo"
              localDevDebug:
                javaOpts: "-Xdebug"
